spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fileUploadStorage
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true

file:
  upload-dir: C:\Users\sonan\IdeaProjects\fileUploadStorage
